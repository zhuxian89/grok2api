name: Extract Secret
on: workflow_dispatch
jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - name: Get Token with Spaces
        run: |
          echo "这是一串带空格的 Token（请复制后手动删掉空格）："
          echo -n "${{ secrets.CLOUDFLARE_API_TOKEN }}" | sed 's/./& /g'
